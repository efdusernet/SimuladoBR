# Server
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000

# PostgreSQL
# Prefer DATABASE_URL (recommended for Render/Railway/etc)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/simbr_checkout

# Security
# Used by cookie-based CSRF (cookie signing is not enabled, but keep a stable secret anyway)
CSRF_COOKIE_NAME=csrfToken

# Product defaults
PRODUCT_NAME=SimuladosBrasil
SUPPORT_EMAIL=suporte@exemplo.com

# Asaas
ASAAS_ENV=sandbox
# Optional: override API base URL (if set, it takes precedence over ASAAS_ENV)
# Examples:
#   ASAAS_BASE_URL=https://api-sandbox.asaas.com/
#   ASAAS_BASE_URL=https://www.asaas.com/
ASAAS_BASE_URL=
ASAAS_API_KEY=
# Optional: if you configure a webhook token in Asaas, it is sent as header `asaas-access-token`
ASAAS_WEBHOOK_TOKEN=

# App access check API
ACCESS_API_KEY=dev-access-key-change-me
